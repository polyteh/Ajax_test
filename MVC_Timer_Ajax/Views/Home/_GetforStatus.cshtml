@model MVC_Timer_Ajax.Models.StringList

<div>
    <h4>StringList</h4>
    <hr />
    <dl class="dl-horizontal">
    </dl>
</div>
<p>
    <table class="table">

        <tr>
            <th>Car name</th>
        </tr>
        @if (Model!=null)
        {
            for (int i = 0; i < Model.status.Count(); i++)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => Model.status[i]);
                    </td>
                </tr>

            }
        }


    </table>
</p>
<p>
    @Html.ActionLink("Edit", "Edit", new { /* id = Model.PrimaryKey */ }) |
    @Html.ActionLink("Back to List", "Index")
</p>
